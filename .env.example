# Orchestrator Private Key (hex format - 64 character hex string)
# Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
ORCHESTRATOR_SK=your_private_key_here

# Nostr Relays (comma-separated WebSocket URLs)
RELAYS=wss://relay.damus.io,wss://nostr.wine,wss://nos.lol

# Known Mints (comma-separated HTTPS URLs, optional for mock mode)
KNOWN_MINTS=https://mint1.example.com,https://mint2.example.com

# Number of hops for transmigration (default: 10)
NUM_HOPS=10

# Mock Mode (true for simulation, false for real mints)
MOCK_MODE=true
